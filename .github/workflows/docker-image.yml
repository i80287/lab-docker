name: Docker Image CI

on: [push, pull_request, workflow_dispatch]

jobs:

  build:

    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]

    steps:
    - uses: actions/checkout@v3
    
    - name: Build the run docker via compose
      run: docker compose up
